package main

// Placeholder file removed from previous refactor. Graceful shutdown logic is
// implemented in `backend/internal/middleware/graceful_shutdown.go` and used
// by `cmd/server/main.go` via the middleware package.
